# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# IDE
.idea/
.vscode/

# Node / web
node_modules/
dist/
.svelte-kit/
*.local

# Downloaded card art (136MB; regenerate with engine/scripts/download_card_images.py)
assets/card_images/cards/

# Regenerable source blob for the OCG card pool (4.6MB; rebuild the CSV with
# `engine/scripts/build_card_db.py --download`). The curated CSV is committed.
assets/card_databases/card_db_ocg_pre_synchro_v6.json

# Recorded duels (replay logs saved by the web server)
replays/

# NOTE: user decks (assets/deck_blueprints/user/), custom banlists
# (assets/banlists/user/) and the player save (/profile/) are intentionally
# tracked so progress syncs across machines via git.

# OS
.DS_Store
